# deduplicate

Deduplicates an array of locations, removing any duplicate entries.

```sig
tileScanner.deduplicate([])
```

## Parameters

* **locations**: The array of locations to deduplicate

```package
arcade-tile-scanner=github:riknoll/arcade-tile-scanner
```
