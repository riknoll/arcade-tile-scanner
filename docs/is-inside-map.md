# is inside map

Returns a TileRule that checks if a location is inside the tilemap or not. This rule is only useful for the "borders" and "borders sides" rules, which will evaluate on locations outside the tilemap.

```sig
tileScanner.isInsideMap()
```

```package
arcade-tile-scanner=github:riknoll/arcade-tile-scanner
```
