# not

Returns a TileRule that matches the inverse of locations matched by the given TileRule.

```sig
tileScanner.not(tileScanner.tileIs(img`.`))
```

## Parameters

* **rule**: The TileRule to invert

```package
arcade-tile-scanner=github:riknoll/arcade-tile-scanner
```
