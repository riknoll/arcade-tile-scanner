# sort by distance

Sorts an array of locations by their distance from a given origin location.

```sig
tileScanner.sortByDistance([], tiles.getTileLocation(0, 0))
```

## Parameters

* **locations**: The array of locations to sort
* **origin**: The origin location to sort by distance from

```package
arcade-tile-scanner=github:riknoll/arcade-tile-scanner
```
