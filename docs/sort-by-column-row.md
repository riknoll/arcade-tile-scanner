# sort by column row

Sorts an array of locations by their column and then row.

```sig
tileScanner.sortByColumnRow([])
```

## Parameters

* **locations**: The array of locations to sort

```package
arcade-tile-scanner=github:riknoll/arcade-tile-scanner
```
