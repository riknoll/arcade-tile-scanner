# tile is

Returns a TileRule that checks if the tile image at a location is a specific tile.

```sig
tileScanner.tileIs(img`.`)
```

## Parameters

* **tile**: The tile image to check for

```package
arcade-tile-scanner=github:riknoll/arcade-tile-scanner
```
